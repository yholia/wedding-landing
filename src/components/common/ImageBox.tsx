import styled from "styled-components";

const ImageBox = styled.div`
    position: relative;
    width: 100%;
    max-width: 400px;
    margin: 0 auto;
    overflow: hidden;
    border-radius: 20px;
`;

export default ImageBox;